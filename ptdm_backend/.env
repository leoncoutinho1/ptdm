# Configuração do Ambiente
ASPNETCORE_ENVIRONMENT=Production
PORT=80 # ou a porta que desejar expor no container/VPS

# Configuração do Banco de Dados (PostgreSQL)
# O TenantService usa esta string como base e troca o nome do Database dinamicamente.
# Certifique-se de que o usuário tenha permissão para acessar os bancos dos tenants.
ConnectionStrings__DefaultConnection=Server=localhost;Port=5432;Database=ptdm;User Id=postgres;Password=SUA_SENHA_AQUI;Pooling=true;

# Configuração JWT (Token de Autenticação)
# ValidAudience e ValidIssuer devem corresponder ao que o frontend espera/envia ou ser validados
JWT__ValidAudience=ptdm
JWT__ValidIssuer=ptdm
# A chave deve ter pelo menos 32 caracteres (256 bits) para ser segura com HMACSHA256
JWT__SecurityKey=SUA_CHAVE_SECRETA_SUPER_SEGURA_E_LONGA_AQUI
JWT__ExpiryInMinutes=60
JWT__RefreshExpiryInMinutes=2880 # 2 dias
FRONTEND_URL=http://localhost:5173